# Frontend

Purpose: Web client (camera capture, UI for identity/liveness/attendance). Communicates with backend API/websocket.

TODO
- Choose stack (e.g., React/Next.js/Vite) and add package scaffold.
- Implement camera capture + frame upload/stream.
- Display verification results, liveness state, attendance logs.
- Handle registration UI (name entry, capture flow) against backend.
